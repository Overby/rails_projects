
<div id="post_show_content" class="skinny_wrapper
wrapper_padding">
  <header>
    <p class="date"><%= @post.created_at.strftime('%A, %B, %d') %></p>
    <h1><%= @post.title %></h1>
    <hr>
  </header>
</div>

<div id="content">
  <%= @post.content %>
  <%#<h2>Narwhal sustainable</h2>
  <p>The refinancing of a primary residence when interest rates decline
    will include the added expense of closing costs.
    However, you can calculate the time period to
    break even with the additional refinancing costs
    (the time period before the actual savings benefits begin).
  </p>
%>
</div>
  <hr>

<div id="share_box">
  <p>Share the DO List</p>
  <!-- Twitter -->
  <a onclick="javascript:window.open('https://twitter.com/share?
  text=<%= @post.title %> by@overbyonline - &amp;url=<%= url_for([@post,
  {only_path: false}]) %>', '_blank', 'width=800, height=500, top=200,
  left=300');void(0);"><i class="fa fa-twitter"></i></a>

  <!-- maybe Medium; which do I want to use? -->
  <!--<a onclick="javascript:window.open('https://twitter.com/share?
    text=<%= @post.title %> by@overbyonline - &amp;url=<%= url_for([@post,
    {only_path: false}]) %>', '_blank', 'width=800, height=500, top=200,
    left=300');void(0);">"><i class="fa fa-medium"></i></a>
  -->

  <!-- LinkedIn here    https://www.linkedin.com/in/andrewdoverby
  <a onclick="javascript:window.open('https://twitter.com/share?
  text=<%= @post.title %> by@overbyonline - &amp;url=<%= url_for([@post,
  {only_path: false}]) %>', '_blank', 'width=800, height=500, top=200,
  left=300');void(0);""><i class="fa fa-linkedin"></i></a>
-->
  <hr>

</div>

<% @post.title %>
<% @post.content %>

<br>
<br>






 link_to "Edit", edit_post_path(@post) %>
<%= link_to "Delete", post_path(@post), method: :delete,
data: {confirm: "Are you sure you want to delete this post?"}
